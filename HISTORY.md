# History

This file is the local running log for conversations and task decisions.

Format for entries:

- `YYYY-MM-DD` - short summary of the request or decision
- Optional: files changed, assumptions, follow-up items

- `2026-05-09` - Added repo-local agent guidance to record concise conversation and task summaries in `HISTORY.md`; created the initial `HISTORY.md` log file.
- `2026-05-09` - Added Graphify support for Codex: installed repo graphify instructions in `AGENTS.md`, created the initial `graphify-out/` graph, installed the git hooks, and added a README note plus ignore rule for generated graph output.
- `2026-05-09` - Completed the Docker Compose dev stack for a Rails API, React/Vite frontend, PostgreSQL 16, and Rswag Swagger scaffolding; added CORS, database env handling, and a starter documented `/up` endpoint.
- `2026-05-09` - Fixed the Docker dev image build failure by adding `libyaml-dev` and `pkg-config` so Ruby's `psych` native extension can compile during `bundle install`.
- `2026-05-09` - Verified the backend container builds successfully after the Dockerfile.dev fix.
- `2026-05-10` - Added a local pre-commit git hook that runs `bundle exec rubocop -A` on staged Ruby files and restages RuboCop changes automatically.
- `2026-05-12` - Finalized the hybrid recurring-task domain design: single-parent lineage, immutable event history, `responsible` naming, `end_reason` for retired nodes, parity-based recurrence selectors, and only one persisted future occurrence per active recurring task; wrote the implementation plan to `docs/superpowers/plans/2026-05-12-hybrid-recurring-task-model.md`.
- `2026-05-12` - Implemented Task 1 of the hybrid recurring-task model: added migrations for `tasks`, `recurrence_rules`, `task_occurrences`, and `task_events`; created core model contracts and focused model specs; ran the model specs in the backend container against the test DB. Note: RSpec needed a temporary fixture-path compatibility shim because `spec/rails_helper.rb` still uses the older `fixture_path=` API.
- `2026-05-12` - Implemented Task 2 recurrence math: added `TaskScheduling::NextOccurrenceCalculator` and `TaskScheduling::CalendarProjection` plus focused specs for one-time, every_n_days, every_n_months, every_n_years, day_of_month_parity, and weekday_parity scheduling. Verified with targeted rspec runs using the local fixture-path shim and the existing PostgreSQL instance under the `exsamption` user.
- `2026-05-12` - Followed up on Task 2 review feedback by making every_n_days DST-safe, tightening recurrence-rule validation for malformed configs, selector ranges, and date-window ordering, and expanding calendar projection coverage for every_n_months, every_n_years, day_of_month_parity, weekday_parity, and DST boundary cases.
- `2026-05-12` - Reviewed the current recurrence scheduling working-tree changes for recurrence math, time zone handling, projection termination, validation contract, and test coverage; targeted specs pass with the temporary fixture-path shim.
- `2026-05-12` - Implemented Task 3 lifecycle services: added `Tasks::AppendEvent`, `Tasks::SplitLineage`, `Tasks::PostponeOccurrence`, and `Tasks::AdvanceOccurrence` with focused service specs; final-state writes use `update_columns` so the existing immutability callback does not block the required terminal transitions. Verified the targeted service specs against the local `exsamption` PostgreSQL role using the temporary RSpec fixture-path shim.
- `2026-05-12` - Reviewed commit `1242a5a` for task lineage and occurrence lifecycle services; noted risks around split occurrence handling, missing split audit events, transition guards, and test coverage. Targeted specs could not run locally because the configured PostgreSQL `postgres` role is unavailable.
- `2026-05-12` - Fixed the Task 3 split/postpone/advance follow-up gaps: `SplitLineage` now locks the parent, appends split/created audit rows, transfers or recreates the planned occurrence, and finalizes the parent with `next_run_at` cleared; `PostponeOccurrence` and `AdvanceOccurrence` now lock and validate source state before mutating. Updated the service specs plus `TaskEvent` coverage, ran the targeted RSpec slice with the `exsamption` DB role and fixture-path shim, and refreshed `graphify-out/`. Assumption: split audit rows can fall back to the parent responsible id when no explicit `actor_id` is provided.
- `2026-05-12` - Reviewed the scoped Task 3 lifecycle services and specs against the hybrid recurring-task spec and plan; found no concrete spec mismatches. Verified the focused service/model specs with the local `exsamption` DB role and temporary RSpec fixture-path shim.
- `2026-05-12` - Reviewed the current task lifecycle service/spec working tree for transaction boundaries, final-state writes, audit consistency, lineage replacement, and test strength. Targeted specs could not run locally because the configured PostgreSQL `postgres` role is unavailable.
- `2026-05-12` - Reviewed the specified task lifecycle working-tree files for code quality, correctness, robustness, and test quality. Key finding: split lineage can leave existing audit events pointing at an occurrence after it is reparented to a child task; targeted specs could not run because the local PostgreSQL `postgres` role is unavailable.
- `2026-05-12` - Fixed the split-lineage audit inconsistency: `SplitLineage` now clones the current occurrence onto the child for responsible changes, supersedes the parent-side occurrence instead of reparenting it, and records explicit source/child occurrence payloads. Updated the split, postpone, and advance specs to assert ordered event history and parent/child occurrence ownership, then reran the focused RSpec slice successfully with the local `exsamption` DB role and fixture-path shim. Refreshed `graphify-out/` afterward.
- `2026-05-14` - Added a compose-backed VS Code devcontainer for the existing backend/frontend/PostgreSQL stack; it bootstraps `.env` from `.env.example` on first launch and reuses the current Docker Compose services.
- `2026-05-14` - Removed the machine-specific Ruby SDK entry from `.idea/medods_test_api.iml` after review feedback so the shared JetBrains module file no longer points at `/usr/local/bin/ruby`.
- `2026-05-14` - Removed the host-side devcontainer bootstrap shell command and switched Compose to use `.env.example` directly so the devcontainer no longer depends on Bash being present on the developer host.
- `2026-05-14` - Reverted the Compose env-file fallback to `.env.example`; the stack now requires a local `.env` again and fails fast if it is missing.
- `2026-05-14` - Strictly reviewed PR #4 (`hybrid-recurring-task-model`) for recurring task correctness, data integrity, edge cases, and verification. Key concerns: final-state transitions are blocked by the immutability callback unless services bypass validations, current occurrence uniqueness ignores postponed rows, calendar projection ignores persisted postponed/current occurrences, and tests could not run against the configured local `postgres` database role. Files changed: `HISTORY.md`.
- `2026-05-14` - Fixed reviewed recurring-task lifecycle defects: finalization now uses normal model updates, current occurrence uniqueness covers planned and postponed statuses in model and DB index, calendar projection includes persisted current occurrences before generated future entries, recurrence rules require recurring tasks, and `SplitLineage` rejects non-structural or incomplete transitions. Added regression specs, kept the RSpec Rails 8 fixture-path shim, verified with full RSpec using local `exsamption` DB role and RuboCop no-cache.
- `2026-05-14` - Addressed follow-up review findings: restored `RAILS_ENV` defaulting in `spec/rails_helper.rb`, made the current-occurrence index migration explicitly reversible with `up`/`down`, and adjusted the test database defaults so plain local `bundle exec rspec` uses the OS user unless DB env vars are provided. Verified migration down/up, plain RSpec, RuboCop no-cache, and diff whitespace.
- `2026-05-14` - Follow-up reviewed the changed files against the prior PR findings. The five domain findings are addressed in code and regression specs, but plain `bundle exec rspec` still fails because `spec/rails_helper.rb` no longer defaults `RAILS_ENV` to `test`; explicit `RAILS_ENV=test DATABASE_USERNAME=exsamption DATABASE_PASSWORD=` RSpec and `bundle exec rubocop --cache false` pass.
- `2026-05-14` - Rechecked after the latest fixes: `spec/rails_helper.rb` now defaults to test, test database defaults use the local OS user unless overridden, the current-occurrence index migration rolls back and reapplies cleanly, plain `bundle exec rspec` passes, and RuboCop no-cache passes.
- `2026-05-14` - Compared `tmp/task.md` with the current `hybrid-recurring-task-model` branch and `tmp/my_takes.txt`. Conclusion: the branch and notes are aligned around a hybrid recurring-task design, but that design still diverges from the assignment’s simpler CRUD + tags + recurrence spec. No code changes were made in this step.
- `2026-05-14` - Wrote assignment-gap docs for `tmp/task.md`: added `docs/superpowers/specs/2026-05-14-task-tracker-api-gap-design.md` and `docs/superpowers/plans/2026-05-14-task-tracker-api-gap-plan.md`. Assumptions: no Devise/auth layer, no extra recurrence modes in the public API, and immutable seeded system tags must be enforced in Rails plus PostgreSQL.
- `2026-05-14` - Revised the task-tracker gap spec and plan after design review: auth/users are now in scope, `title` should become `name`, recurrence is assignment-minimum plus documented extensions, business rows are deactivated rather than deleted, and delegated tasks use `pending_acceptance` with accept/decline actions. `responsible_id` may be null only when `creator_id` or `delegated_user_id` preserves task ownership context.
- `2026-05-14` - Added task visibility authorization to the gap docs: administrators can view every task, while doctors and nurses can view only tasks bonded to them by `creator_id`, `responsible_id`, or `delegated_user_id`.
- `2026-05-14` - Renamed the planned tag boolean from `system_tag` to `is_system_tag` in the task-tracker gap spec and implementation plan for clearer boolean naming.
- `2026-05-14` - Tightened the gap spec wording around non-destructive removal: acceptance criteria now says removal behavior deactivates records and does not remove database rows.
- `2026-05-14` - Rechecked the implementation plan against the gap spec and fixed drift: the task migration plan now includes `completion_date`, and the plan uses removal wording for non-destructive deactivation.
- `2026-05-14` - Completed Task 1 auth groundwork: replaced the temporary bcrypt-backed password path with a first-party salted digest, exposed `current_user`/`authenticate_user!` on `ApplicationController`, added `/api/v1/auth/me` coverage, and updated the users migration/schema plus history for the new auth contract.
- `2026-05-14` - Addressed Task 1 review feedback: switched password hashing to PBKDF2-HMAC-SHA256 with explicit constants, added plaintext-digest regression coverage, expanded auth request specs for invalid login and bearer-token failures, and added email normalization/duplicate-email coverage.
- `2026-05-14` - Implemented Task 2 Task-model public API alignment: renamed `tasks.title` to `tasks.name`, added `completion_date`, `creator_id`, nullable `responsible_id`, `delegated_user_id`, `accepted_at`, `cancellation_reason`, `deactivated_at`, and the `declined` end reason; added user associations plus the ownership invariant, updated task-domain specs/services to use user records and `name`, applied the migration, and verified the full RSpec suite.
- `2026-05-14` - Hardened Task 2 after strict review: the public-API migration now seeds placeholder users for legacy `responsible_id` values before adding the FK, rollback backfills `responsible_id` from creator/delegated ownership instead of nulling blindly, user destroy now raises instead of nullifying task ownership, and `Task#retire!` accepts explicit cancellation metadata while keeping sensible defaults. Verified with focused model/migration specs, the task-domain slice, and the full RSpec suite.
- `2026-05-14` - Implemented the task CRUD/list/deactivation request API slice: added bearer-authenticated `/api/v1/tasks` REST routes and `Api::V1::TasksController`, with default `creator=current_user`, soft deactivation via `deactivated_at`, bonded visibility for staff users, admin-wide access, and date/scope filtering over `completion_date`, `first_run_at`, and `next_run_at`. Added focused request coverage in `spec/requests/api/v1/tasks_spec.rb`.
- `2026-05-15` - Implemented Task 4 task acceptance/decline endpoints: added `Api::V1::TaskAcceptancesController`, nested `POST /api/v1/tasks/:task_id/accept` and `/decline` routes, delegate-only access, locked accept/decline transitions, stale deactivation rechecks, and audit events. Verified locally with full RSpec (`83 examples, 0 failures`) and RuboCop no-cache.
- `2026-05-15` - Implemented Task 5 tags and task-tag linking: added `Tag`/`TaskTag`, soft-deactivation joins with reactivation, seeded the required system tags, added authenticated tag and task-tag request APIs, and added Rails/PostgreSQL immutability protections for seeded system tags.
- `2026-05-15` - Implemented Task 6 specific-date recurrence support: added `RecurrenceRuleDate`, the `specific_dates` recurrence mode, nested date validations, calculator/projection support for explicit run dates, and the new migration plus schema/structure snapshots. Verified with focused recurrence RSpec and database preparation under the local `exsamption` PostgreSQL role.
- `2026-05-15` - Implemented Task 7 occurrence actions: added authenticated `task_occurrences` postpone/execute/skip routes and controller, a skip service, occurrence-level request coverage, and small occurrence/service guard helpers. Assumption: delegated-only pending users stay read-only until acceptance; skip keeps the task series moving without mutating sibling occurrences.
- `2026-05-18` - Reviewed the `Task` timestamp lifecycle and traced each field through the model, task acceptance flow, occurrence services, and API presenter. Clarified which fields are operational scheduling state (`first_run_at`, `next_run_at`), transition markers (`accepted_at`, `completed_at`, `cancelled_at`), visibility/control state (`deactivated_at`), and user-facing planning data (`completion_date`).
- `2026-05-15` - Implemented Task 8 API documentation: expanded README assumptions, added executable Rswag coverage for auth/tasks/delegation/tags/occurrences, configured bearer auth schemas, and generated `swagger/v1/swagger.json`.
- `2026-05-15` - Finalized schema handling for the public API gap implementation: switched the authoritative schema dump to `db/structure.sql` so PostgreSQL trigger constraints are preserved, and removed the stale `db/schema.rb` artifact.
- `2026-05-15` - Split the completed public task API gap implementation into local atomic commits by planning docs, auth, task delegation flow, tags/non-destructive removal, recurrence/occurrence actions, and API documentation. Left unrelated `.idea/medods_test_api.iml` changes uncommitted.
- `2026-05-15` - Resolved the README merge conflict by merging the local startup, Swagger, Graphify, and Dev Container guidance into one document and keeping the `.env` requirement explicit.
- `2026-05-15` - Fixed PR #4 review findings: blocked all `Task` hard deletes, enabled public delegated/recurring task creation with initial occurrence scheduling, projected date-filtered task lists at occurrence level with `occurrence_status` filtering, completed recurring series when the last occurrence is skipped, and made delegation decline clear `next_run_at` plus cancel current occurrences. Verified full RSpec and RuboCop no-cache.
- `2026-05-15` - Corrected the residual lifecycle `status` filter follow-up into a Superpowers spec at `docs/superpowers/specs/2026-05-15-task-list-status-filter-design.md` and removed the mistaken request-spec-only change. Next step is to implement controller-side status filtering from that spec.
- `2026-05-15` - Implemented the task-list lifecycle `status` query filter for `GET /api/v1/tasks`: validation now returns 400 for unknown statuses, filtering composes with scope and date/occurrence projection, and request specs cover the public contract. Verified `spec/requests`.
- `2026-05-15` - Created the frontend upgrade spec for matching the new backend task API features at `docs/superpowers/specs/2026-05-15-frontend-backend-feature-upgrade-design.md`. Decision: use a hybrid role-based frontend where staff and administrators share the task workspace, with admin-only capabilities layered by role.
- `2026-05-15` - Accepted the hybrid frontend upgrade spec and created the implementation plan at `docs/superpowers/plans/2026-05-15-frontend-backend-feature-upgrade.md`. Plan covers auth/session, role-aware navigation, task filters, recurrence creation, occurrence actions, tags, admin views, tests, and verification.
- `2026-05-16` - Advanced the hybrid frontend upgrade implementation across the React app: added auth/session handling, role-aware shell navigation, task filtering and calendar projection, task creation/detail/occurrence actions, tag maintenance, and the admin view. Verified with the full frontend Vitest suite and Vite build; next step is final polish/merge cleanup if needed.
- `2026-05-16` - Investigated the reported Rswag command failure. The valid command `SWAGGER_DRY_RUN=0 bundle exec rspec spec/requests/api/v1/swagger_spec.rb --format Rswag::Specs::SwaggerFormatter` passes locally with 11 examples and regenerates `swagger/v1/swagger.json`; appending prose words such as `returns errors` makes RSpec treat them as spec file paths and fail while loading `./returns` and `./errors`.
- `2026-05-16` - Fixed the devcontainer-only Rswag failures: the backend service runs with `RAILS_ENV=development`, so request specs hit Rails host authorization and `www.example.com` was blocked. Added a `localhost` request host in `spec/requests/api/v1/swagger_spec.rb`; verified the swagger spec passes both inside the Compose backend container and on the host.
- `2026-05-16` - Reviewed PR #6 strictly. Findings: recurring task creation can still raise on missing recurrence rules, and the frontend occurrence action flow drops occurrence context when opening projected/list rows. Verified targeted backend request specs and frontend Vitest suite.
- `2026-05-16` - Fixed the PR #6 review findings: the task create flow now rejects recurring tasks that have no recurrence rule and no initial schedule before scheduling starts, and task list navigation now preserves occurrence context into task detail so execute/postpone/skip controls stay reachable. Added regression coverage on both sides.
- `2026-05-17` - Tightened the recurring-task create guard after follow-up review: `first_run_at` no longer counts as a standalone escape hatch, so recurring creates now require `recurrence_rule_attributes` unless `next_run_at` is explicitly used as the initial occurrence. Added a request-spec regression for the `first_run_at`-only case.
- `2026-05-17` - Rechecked the modified files for the PR #6 finding fixes. The first-run-only recurring create path now returns 422, targeted backend/frontend tests pass, and frontend occurrence context is preserved for in-app navigation via router state.
- `2026-05-17` - Fixed the frontend recurrence gap for `weekday_parity`: `RecurrenceFields` now renders an accessible even/odd selector when that rule type is chosen, and `TaskForm` already forwards `weekday_parity` into `recurrence_rule_attributes`. Added regression tests for field visibility and payload submission, then reran the focused frontend Vitest slice successfully.
- `2026-05-17` - Fixed two Task API review findings with TDD: date-filtered `GET /api/v1/tasks` now narrows candidate tasks in SQL and preloads only in-range occurrences before recurrence projection, and task payloads now include active attached tags for the frontend tag UI. Verified with focused request specs and the full `spec/requests/api/v1/tasks_spec.rb` file.
- `2026-05-17` - Fixed TaskDetail PR findings with strict TDD: occurrence state now lives locally and is refreshed from occurrence transition responses so execute/postpone/skip no longer leave stale navigation-state UI behind, and clearing completion date now sends `null` to `updateTask`. Files changed: `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskDetail.test.jsx`. Verified with focused Vitest run for `TaskDetail.test.jsx`.
- `2026-05-17` - Fixed the one-time task date projection regression in `Api::V1::TasksController`: date-filtered list responses now project only `next_run_at || first_run_at` for one-time tasks, so a task with both timestamps appears once at the active scheduled time instead of duplicating planned rows. Added a focused request-spec regression and updated the broader date-filter expectation accordingly.
- `2026-05-17` - Fixed the recurrence parity payload gap in the frontend task form: visible-default `even` now submits for both `day_of_month_parity` and `weekday_parity` when unchanged, while explicit `odd` selection remains unchanged. Added focused Vitest regressions and verified the file with `npm -C frontend test -- TaskForm.test.jsx`.
- `2026-05-17` - Fixed task payload drift: added request-spec coverage proving occurrence and acceptance transitions return active tags in the same shape as task responses, extracted `Api::V1::TaskPayloadPresenter`, and routed `TasksController`, `TaskOccurrencesController`, and `TaskAcceptancesController` through it. Verified with transition request specs and the full task request spec.
- `2026-05-17` - Fixed the latest two task-list PR findings with strict TDD: executed persisted occurrences now suppress duplicate planned projections by scheduled slot, and recurring tasks with only `next_run_at` now appear in date-filtered lists as projected planned rows. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `spec/requests/api/v1/tasks_spec.rb`, `HISTORY.md`. Verified with focused request-spec failures first, then green focused and full `spec/requests/api/v1/tasks_spec.rb`.
- `2026-05-17` - Performed a full repository code review using `code_review.md` layers. Verified RSpec, Vitest, Brakeman, and RuboCop; noted npm audit dev-dependency advisory and API/auth hardening findings. Files changed: `HISTORY.md`.
- `2026-05-17` - Saved the full code review findings as unchecked checklist items in `tmp/code_reviews/20260517_050339.md`. Files changed: `tmp/code_reviews/20260517_050339.md`, `HISTORY.md`.
- `2026-05-17` - Fixed two TasksController query-parameter findings with strict TDD: `GET /api/v1/tasks` now rejects date-filter windows over 31 days and validates `occurrence_status` against `TaskOccurrence.statuses`, returning 400 for invalid input instead of empty 200s. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `spec/requests/api/v1/tasks_spec.rb`, `HISTORY.md`. Verified with focused red/green request specs and the full `spec/requests/api/v1/tasks_spec.rb`.
- `2026-05-17` - Fixed the `/api/v1/auth/login` unlimited password-check finding with Rack::Attack throttling keyed by IP plus normalized email. Added a focused request-spec regression for repeated failures from the same IP/email bucket, wired Rack::Attack into the middleware stack, and refreshed `graphify-out/`. Verified with `bundle exec rspec spec/requests/api/v1/auth_spec.rb` after the red-green cycle. Files changed: `Gemfile`, `Gemfile.lock`, `config/application.rb`, `config/initializers/rack_attack.rb`, `spec/requests/api/v1/auth_spec.rb`, `HISTORY.md`.
- `2026-05-17` - Updated the frontend toolchain to remove the Vite/esbuild audit finding by bumping `vite` to `^6.4.2`, `@vitejs/plugin-react` to `^5.2.0`, and `vitest` to `^4.1.6`. Verified with `npm -C frontend audit` (0 vulnerabilities), `npm -C frontend test`, and `npm -C frontend run build`. Files changed: `frontend/package.json`, `frontend/package-lock.json`, `HISTORY.md`.
- `2026-05-17` - Made the frontend friendlier and Russian-language focused: localized navigation/forms/task/tag/admin copy, replaced the calendar list shortcut with a month calendar that queries the visible month, improved task row metadata, and added calendar regression tests. Verified with frontend Vitest and Vite build. Files changed: `frontend/src/**`, `HISTORY.md`.
- `2026-05-17` - Fixed task filter alignment after UI review: scoped the filter bar to a grid layout with consistent label/input/helper rows so occurrence status and date fields line up. Verified with the task-list Vitest slice and Vite build. Files changed: `frontend/src/tasks/TaskFilters.jsx`, `frontend/src/styles.css`, `HISTORY.md`.
- `2026-05-17` - Fixed task creation form alignment after UI review: scoped the first-row task form grid so labels reserve a consistent row height and long labels no longer push inputs down. Verified with the task-form Vitest slice and Vite build. Files changed: `frontend/src/tasks/TaskForm.jsx`, `frontend/src/styles.css`, `HISTORY.md`.
- `2026-05-17` - Changed task creation defaults so self-assigned tasks now start as `ongoing` while delegated tasks still start as `pending_acceptance`. Updated the public task creation request spec accordingly and verified the task request spec slice. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `spec/requests/api/v1/tasks_spec.rb`, `HISTORY.md`.
- `2026-05-17` - Fixed dev backend container startup after adding `rack-attack`: the compose `bundle-cache` volume can hide newly installed image gems, so backend startup now runs `bundle check || bundle install` before `db:prepare` and Rails server boot. Verified the missing-gem failure first with `docker compose run --rm --no-deps backend sh -lc 'bundle check'`.
- `2026-05-17` - Added idempotent dev/test seed data with Faker: system tags now always include `Отчётность`, `Операции`, and `Звонок`; demo data now seeds common tags, 3 doctors, 3 nurses, and a mixed task set with recurrence rules, occurrences, and events. Files changed: `Gemfile`, `Gemfile.lock`, `db/seeds.rb`, `db/seeds/medods_demo_data.rb`, `graphify-out/GRAPH_REPORT.md`.
- `2026-05-17` - Reworked the frontend toward the new dense operator UI: updated the dark sidebar shell, converted the task list to a table layout, refreshed the task detail into a two-column inspector view, restyled the calendar/tags/admin/login surfaces, and adjusted the tag catalog test for the new table cells. Verified with focused frontend Vitest and Vite build, then refreshed `graphify-out/`. Files changed: `frontend/src/admin/AdminPage.jsx`, `frontend/src/auth/LoginPage.jsx`, `frontend/src/layout/Shell.jsx`, `frontend/src/styles.css`, `frontend/src/tags/TagsPage.jsx`, `frontend/src/tags/TagsPage.test.jsx`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskList.jsx`, `HISTORY.md`.
- `2026-05-17` - Applied a small visual polish pass on the redesign: tightened sidebar spacing, clarified the active-nav state, added sticky table headers and alternating row shading for task/tag tables, and reduced some surface padding for denser scanning. Files changed: `frontend/src/styles.css`, `HISTORY.md`.
- `2026-05-17` - Implemented the frontend form/filter/calendar follow-up rules: removed the redundant delegated tab, renamed scope to `Принадлежность`, defaulted task filters to today, switched user displays to role-based labels, added admin-only `/api/v1/users`, replaced the delegated-ID input with a user dropdown, made recurrence next-run previews/read-only, deduped time zone options, and updated calendar/task/detail formatting. Verified with focused frontend Vitest, `bundle exec rspec spec/requests/api/v1/users_spec.rb spec/requests/api/v1/tasks_spec.rb`, `npm -C frontend run build`, and refreshed `graphify-out/`. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `app/controllers/api/v1/users_controller.rb`, `app/models/task.rb`, `app/models/user.rb`, `app/presenters/api/v1/task_payload_presenter.rb`, `frontend/src/App.jsx`, `frontend/src/calendar/CalendarPage.jsx`, `frontend/src/layout/Shell.jsx`, `frontend/src/recurrence/RecurrenceFields.jsx`, `frontend/src/styles.css`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskForm.jsx`, `frontend/src/tasks/TaskList.jsx`, `frontend/src/utils/display.js`, `spec/requests/api/v1/tasks_spec.rb`, `spec/requests/api/v1/users_spec.rb`, `HISTORY.md`.
- `2026-05-17` - Fixed the follow-up regressions after the first implementation pass: admin task lists no longer inherit the default "from today" filter, assignee controls now align on one row, and the remaining date/time pickers were converted to text-based `DD-MM-YYYY` / `HH:MM` fields with strict parsing. Verified with focused frontend Vitest and Vite build, then refreshed `graphify-out/`. Files changed: `frontend/src/admin/AdminPage.jsx`, `frontend/src/recurrence/RecurrenceFields.jsx`, `frontend/src/styles.css`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskFilters.jsx`, `frontend/src/tasks/TaskForm.jsx`, `frontend/src/tasks/TaskForm.test.jsx`, `frontend/src/tasks/TaskList.jsx`, `frontend/src/utils/display.js`, `HISTORY.md`.
- `2026-05-17` - Reverted the date-picker text-input patch on request: restored native `date`, `datetime-local`, and `time` controls in the task form/detail/filter/recurrence UI, removed the temporary date/time input parsing helpers, and reset the task-form regression test data back to ISO-style native picker values. Verified with focused frontend Vitest and Vite build. Files changed: `frontend/src/recurrence/RecurrenceFields.jsx`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskFilters.jsx`, `frontend/src/tasks/TaskForm.jsx`, `frontend/src/tasks/TaskForm.test.jsx`, `frontend/src/utils/display.js`, `HISTORY.md`.
- `2026-05-18` - Redesigned the auth entrypoint to match the new mock: added a centered glassy login panel with password reveal control, converted the secondary CTA into a real `/register` flow, added backend auth registration plus frontend auth context/endpoints, and added auth page coverage for login/register/redirect behavior. Verified with focused frontend Vitest, `bundle exec rspec spec/requests/api/v1/auth_spec.rb`, `bundle exec rspec spec/requests/api/v1/swagger_spec.rb`, `npm -C frontend run build`, and a Chromium screenshot pass on `/login` and `/register`. Files changed: `app/controllers/api/v1/auth_controller.rb`, `app/models/user.rb`, `config/routes.rb`, `frontend/src/App.jsx`, `frontend/src/api/endpoints.js`, `frontend/src/auth/AuthContext.jsx`, `frontend/src/auth/AuthPage.jsx`, `frontend/src/auth/LoginPage.jsx`, `frontend/src/auth/LoginPage.test.jsx`, `frontend/src/auth/PasswordField.jsx`, `frontend/src/auth/RegisterPage.jsx`, `frontend/src/auth/RegisterPage.test.jsx`, `frontend/src/styles.css`, `spec/requests/api/v1/auth_spec.rb`, `spec/requests/api/v1/swagger_spec.rb`, `HISTORY.md`.
- `2026-05-18` - Fixed the recurring-task create crash caused by a blank `date_start`: `Task#initial_recurring_run_at` and the create-path scheduler now bail out when the recurrence rule lacks a start date, so the request returns the existing validation error instead of raising `NoMethodError`. Added a regression to `spec/requests/api/v1/tasks_spec.rb` and verified the task request-spec slice. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `app/models/task.rb`, `spec/requests/api/v1/tasks_spec.rb`, `HISTORY.md`.
- `2026-05-18` - Implemented the task scheduling visibility fixes: self-assigned creates now normalize `first_run_at`/`next_run_at` before save, `/tasks` defaults to a 31-day window plus the default-list unscheduled flag, recurrence payloads now expose `date_start`/`date_end`, the task detail/list/calendar UIs show recurrence end dates separately from completion dates, and calendar projection suppresses duplicate recurrence times. Verified with `bundle exec rspec spec/requests/api/v1/tasks_spec.rb spec/services/task_scheduling/calendar_projection_spec.rb spec/services/task_scheduling/next_occurrence_calculator_spec.rb`, `npm test -- TaskForm.test.jsx TaskList.test.jsx CalendarPage.test.jsx`, and `npm run build`. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `app/presenters/api/v1/task_payload_presenter.rb`, `app/services/task_scheduling/calendar_projection.rb`, `frontend/src/tasks/TaskForm.jsx`, `frontend/src/tasks/TaskList.jsx`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/calendar/CalendarPage.jsx`, `frontend/src/styles.css`, `spec/requests/api/v1/tasks_spec.rb`, `spec/services/task_scheduling/calendar_projection_spec.rb`, `frontend/src/tasks/TaskForm.test.jsx`, `frontend/src/tasks/TaskList.test.jsx`, `frontend/src/calendar/CalendarPage.test.jsx`, `HISTORY.md`.
- `2026-05-18` - Implemented recurring end-date sync: recurring task saves now mirror `completion_date` and `recurrence_rule.date_end` in both directions, legacy rows backfill the missing side, recurrence projection falls back to the task completion date so `128`-style series stop at the visible end date, and the request/spec slices cover create/update plus the inclusive cutoff. Verification passed with `bundle exec rspec spec/requests/api/v1/tasks_spec.rb spec/services/task_scheduling/calendar_projection_spec.rb spec/services/task_scheduling/next_occurrence_calculator_spec.rb`, `npm -C frontend test -- TaskDetail.test.jsx TaskForm.test.jsx TaskList.test.jsx CalendarPage.test.jsx`, and `npm -C frontend run build`. Note: `docker compose exec backend bundle exec rails db:migrate` hit a local `pg_dump` version mismatch after the migration applied, so the test DB migration version was recorded directly to keep the suite in sync. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `app/models/recurrence_rule_date.rb`, `app/models/task.rb`, `app/presenters/api/v1/task_payload_presenter.rb`, `app/services/task_scheduling/next_occurrence_calculator.rb`, `db/migrate/20260518000001_sync_recurring_task_end_dates.rb`, `frontend/src/tasks/TaskDetail.jsx`, `spec/requests/api/v1/tasks_spec.rb`, `spec/services/task_scheduling/calendar_projection_spec.rb`, `spec/services/task_scheduling/next_occurrence_calculator_spec.rb`, `HISTORY.md`.
- `2026-05-18` - Fixed the backend Docker images to install `postgresql-client-16` from PGDG so Rails schema dumps match the PostgreSQL 16 server and `rails db:migrate` no longer trips over a 15.x `pg_dump`. Updated both `Dockerfile.dev` and `Dockerfile` and kept the repo history current. Files changed: `Dockerfile.dev`, `Dockerfile`, `HISTORY.md`.
- `2026-05-18` - Reviewed PR `#7` (`frontend-redesign-medods-operator-ui`) end-to-end. Confirmed the focused frontend Vitest slices pass locally, but backend RSpec could not run in the sandbox because PostgreSQL on `127.0.0.1:5432` is blocked here. Current findings: planned-occurrence projection still includes final tasks without a date range, recurring task edits from the detail UI can fail for legacy recurring tasks that have no `recurrence_rule`, and the new recurring end-date validation rejects same-day start/end series. Files changed: `HISTORY.md`. Next step: fix the three review findings and rerun the affected request/service specs in an environment with database access.
- `2026-05-18` - Reproduced the backend RSpec suite inside the running `backend` container after the review follow-up edits. The targeted task/model slices now pass, and the three review findings appear fixed in code. The remaining full-suite failures cluster into separate issues: Rails test host authorization is blocking request specs that still use `www.example.com`, and several older specs assert global `Task`/`TaskTag`/`TaskEvent` counts instead of scoping to per-example deltas. Files changed: `HISTORY.md`. Next step: allow the default test host in `config/environments/test.rb` and tighten the brittle count assertions.
- `2026-05-18` - Fixed the three PR review regressions from the recurring-task end-date work: rule-less legacy recurring tasks are now editable from the detail screen without forcing nested recurrence attrs, the no-range planned-occurrence listing skips inactive final tasks, and same-day recurring series are accepted by both the form and backend validation. Added request/model/frontend regressions and kept the history current. Files changed: `app/controllers/api/v1/tasks_controller.rb`, `app/models/task.rb`, `frontend/src/tasks/TaskDetail.jsx`, `frontend/src/tasks/TaskDetail.test.jsx`, `frontend/src/tasks/TaskForm.jsx`, `frontend/src/tasks/TaskForm.test.jsx`, `spec/models/task_spec.rb`, `spec/requests/api/v1/tasks_spec.rb`, `HISTORY.md`.
- `2026-05-18` - Fixed the remaining full-suite test failures after the PR review follow-up: affected request specs now pin `host! "localhost"` instead of relying on the default blocked host, brittle migration/task-tag/task-event expectations now assert scoped rows or per-example count deltas, the users request spec no longer assumes the newly created admin is the first admin in the whole dataset, and `graphify-out/` was refreshed. Verified with the previously failing backend RSpec slice and the full `docker compose exec backend bundle exec rspec` suite (`182 examples, 0 failures`), plus focused frontend Vitest for `TaskDetail` and `TaskForm`. Files changed: `config/environments/test.rb`, `spec/migrations/update_tasks_for_public_api_spec.rb`, `spec/requests/api/v1/auth_spec.rb`, `spec/requests/api/v1/task_tags_spec.rb`, `spec/requests/api/v1/users_spec.rb`, `spec/requests/up_spec.rb`, `spec/services/tasks/append_event_spec.rb`, `graphify-out/GRAPH_REPORT.md`, `graphify-out/graph.json`, `graphify-out/graph.html`, `HISTORY.md`.
